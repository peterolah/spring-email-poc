spring:
    application:
        name: email
    mail:
        host: smtp.gmail.com
        password: ${GOOGLE_SMTP_APP_PASSWORD}
        port: 587
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
        username: rehabit.munka@gmail.com
